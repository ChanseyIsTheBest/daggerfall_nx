/* asset_pack.h -- forwarder. libc_shim.c is compiled in unmodified and
 * includes this by name; everything it declares lives in compat_stubs.h.
 * See the header comment there. */
#ifndef PPS_FWD_ASSET_PACK_H
#define PPS_FWD_ASSET_PACK_H
#include "compat_stubs.h"
#endif
